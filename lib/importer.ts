import * as XLSX from "xlsx"; import Papa from "papaparse"; export const digits=(v:any)=>String(v??"").replace(/\\D/g,""); export const phone=(v:any)=>{let d=digits(v);if(d.length===10||d.length===11)d="55"+d;return d}; export const cpf=(v:any)=>digits(v); const norm=(v:any)=>String(v??"").normalize("NFD").replace(/[\\u0300-\\u036f]/g,"").toLowerCase().trim(); const aliases:any={nome:["nome","cliente","beneficiario","beneficiário","name"],cpf:["cpf","documento"],telefone:["telefone","celular","fone","fone1","telefone1","phone"],telefone2:["telefone2","celular2","fone2"],cidade:["cidade","municipio","município"],uf:["uf","estado"],produto:["produto","modalidade"],observacao:["observacao","observação","obs"]}; export async function parseFile(file:File){const ext=file.name.split(".").pop()?.toLowerCase()||"";let rows:any[]=[];if(ext==="csv"||ext==="txt"){const r=Papa.parse(await file.text(),{header:true,skipEmptyLines:true});rows=(r.data||[]) as any[]}else{const wb=XLSX.read(await file.arrayBuffer(),{type:"array",cellDates:true});rows=XLSX.utils.sheet_to_json(wb.Sheets[wb.SheetNames[0]],{defval:""})}const cols=Array.from(new Set(rows.flatMap(r=>Object.keys(r))));const find=(names:string[])=>cols.find(x=>names.includes(norm(x)));const map:any={};Object.keys(aliases).forEach(k=>map[k]=find(aliases[k]));const parsed=rows.map(r=>({nome:map.nome?r[map.nome]:"",cpf:cpf(map.cpf?r[map.cpf]:""),telefone:phone(map.telefone?r[map.telefone]:""),telefone2:phone(map.telefone2?r[map.telefone2]:""),cidade:map.cidade?r[map.cidade]:"",uf:map.uf?r[map.uf]:"",produto:map.produto?r[map.produto]:"",observacao:map.observacao?r[map.observacao]:"",extras:Object.fromEntries(Object.entries(r).filter(([k])=>!Object.values(map).includes(k)))}));return{ext,cols,map,rows:parsed,total:parsed.length,validos:parsed.filter(x=>x.nome&&x.telefone).length}}